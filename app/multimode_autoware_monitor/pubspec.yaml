name: multimode_autoware_monitor
description: Multi-Mode Autoware Monitor — localization multi-mode / pipeline / Autoware stack monitor for ROS 2 + Autoware + CARLA.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^3.0.1

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - config/mode_catalog.json
    - config/roii_vehicle_topology.json
